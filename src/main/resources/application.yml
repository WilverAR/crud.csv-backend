server:
  port: 8080

spring:
  application:
    name: CRUD.CSV

  datasource:
    url: jdbc:mysql://server-sql.mysql.database.azure.com:3306/resale?useSSL=true
    username: admin_azure
    password: WarAe*280101
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false