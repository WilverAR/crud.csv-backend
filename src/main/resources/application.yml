server:
  port: 8080

spring:
  application:
    name: CRUD.CSV

  datasource:
    url: jdbc:mysql://s-mysql.mysql.database.azure.com:3306/resale?useSSL=true&serverTimezone=UTC
    username: wilverar
    password: warraw*280101
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC